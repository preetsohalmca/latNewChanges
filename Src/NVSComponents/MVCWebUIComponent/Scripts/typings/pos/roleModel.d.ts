declare namespace Volvo.LAT.Models {

    /**
    * Defines a contract for the user role model.
    * It must be in sync with the RoleModel from C#
    */
    export interface RoleModel {
        Number: number;
        Name: string;
    }

}