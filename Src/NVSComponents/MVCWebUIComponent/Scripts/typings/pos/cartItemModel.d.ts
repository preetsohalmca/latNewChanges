declare namespace Volvo.LAT.Models {

    /**
     * Defines a contract for the cart item model.
     * It must be in sync with the CartItemModel from C#
     */
    export interface CartItemModel {
        PartNumber: number;
        Quantity: number;
    }

}